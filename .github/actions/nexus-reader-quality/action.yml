name: Nexus Reader Quality
description: Setup Bun, install nexus-reader dependencies, and run selected quality checks.

inputs:
  bun-version:
    description: Bun version to install.
    required: false
    default: "1"
  working-directory:
    description: Path to the nexus-reader workspace.
    required: false
    default: nexus-reader
  run-lint:
    description: Whether to run lint:check.
    required: false
    default: "false"
  run-type-check:
    description: Whether to run the frontend type check.
    required: false
    default: "false"
  run-tests:
    description: Whether to run frontend tests.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: ${{ inputs.bun-version }}

    - name: Install dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        if [ -f bun.lock ] || [ -f bun.lockb ]; then
          bun install --frozen-lockfile
        else
          bun install
        fi

    - name: Run lint
      if: ${{ inputs.run-lint == 'true' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: bun run lint:check

    - name: Run type check
      if: ${{ inputs.run-type-check == 'true' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: bun run type-check

    - name: Run tests
      if: ${{ inputs.run-tests == 'true' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: bun run test
