# Nexus Reader 飞牛OS 环境配置

# 基础配置
COMPOSE_PROJECT_NAME=nexus-reader
COMPOSE_FILE=docker-compose.fnos.yml

# 时区配置
TZ=Asia/Shanghai

# 端口配置（如有冲突可修改）
NEXUS_PORT=8080
CF_BYPASS_PORT=8000

# 数据目录
DATA_DIR=./data

# 镜像标签
IMAGE_TAG=fnos-latest