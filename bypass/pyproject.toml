[project]
name = "bypass-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cloudscraper>=1.2.70",
    "curl-cffi>=0.7.0",
    "fastapi>=0.104.0",
    "playwright>=1.40.0",
    "pydantic>=2.5.0",
    "uvicorn>=0.24.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "hypothesis>=6.0.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=0.21.0",
]
