{
  "name": "nexus-edge",
  "version": "1.0.0",
  "description": "Nexus Reader Edge Worker — proxy, caching, CORS",
  "main": "entry.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "status": "wrangler deployments list",
    "test": "vitest run",
    "type-check": "node ./scripts/type-check.mjs",
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "contracts:generate": "node ../scripts/generate-http-route-constants.mjs",
    "contracts:validate": "node ../scripts/validate-contracts.mjs"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "novel",
    "decoder",
    "optimization",
    "ai"
  ],
  "author": "Nexus Reader",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.127.0"
  }
}
