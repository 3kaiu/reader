# Clippy configuration (supported fields only).

# Keep complexity caps explicit so defaults don't drift across toolchain versions.
cognitive-complexity-threshold = 30
type-complexity-threshold = 250
